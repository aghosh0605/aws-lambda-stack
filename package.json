{
  "name": "replaced-by-user-input",
  "description": "replaced-by-user-input",
  "version": "0.0.1",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "@types/node": "^18.15.3",
    "jest": "^29.5.0",
    "typescript": "^5.0.2"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "local": "sam build --use-container && sam local start-api",
    "deploy": "sam deploy --guided",
    "debug": "sam validate --debug"
  },
  "files": [
    "dist/",
    "tsconfig.json"
  ]
}
